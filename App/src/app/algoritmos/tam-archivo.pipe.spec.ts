import { TamArchivoPipe } from './tam-archivo.pipe';

describe('TamArchivoPipe', () => {
  it('create an instance', () => {
    const pipe = new TamArchivoPipe();
    expect(pipe).toBeTruthy();
  });
});

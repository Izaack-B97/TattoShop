import { TamanoArchivoPipe } from './tamano-archivo.pipe';

describe('TamanoArchivoPipe', () => {
  it('create an instance', () => {
    const pipe = new TamanoArchivoPipe();
    expect(pipe).toBeTruthy();
  });
});
